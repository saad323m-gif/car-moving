/* إعدادات عامة */
* {
  box-sizing: border-box;
  -webkit-tap-highlight-color: transparent;
}

html, body {
  margin: 0;
  padding: 0;
  font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  background-color: #f3f4f6;
  color: #111827;
}

/* أزرار عامة */
button {
  font-family: inherit;
}

.btn-primary,
.btn-secondary,
.btn-danger {
  border: none;
  border-radius: 999px;
  padding: 0.5rem 1rem;
  font-size: 0.9rem;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 0.25rem;
}

.btn-primary {
  background: linear-gradient(135deg, #2563eb, #1d4ed8);
  color: #fff;
}

.btn-secondary {
  background-color: #e5e7eb;
  color: #111827;
}

.btn-danger {
  background-color: #dc2626;
  color: #fff;
}

/* حالة مخفية */
.hidden {
  display: none !important;
}

/* ================= شاشة الدخول ================= */

.auth-view {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 1.5rem;
}

.auth-card {
  width: 100%;
  max-width: 380px;
  background-color: #ffffff;
  border-radius: 16px;
  box-shadow: 0 15px 35px rgba(15, 23, 42, 0.18);
  padding: 1.5rem 1.75rem 1.75rem;
  text-align: center;
}

.auth-logo img {
  width: 72px;
  height: 72px;
  object-fit: contain;
  margin-bottom: 0.5rem;
}

.auth-title {
  margin: 0;
  font-size: 1.15rem;
  font-weight: 700;
  color: #111827;
}

.auth-subtitle {
  margin: 0.15rem 0 0.8rem;
  font-size: 0.8rem;
  color: #6b7280;
}

/* تبويبات الدخول / التسجيل */
.auth-tabs {
  display: flex;
  background-color: #f3f4f6;
  border-radius: 999px;
  padding: 0.15rem;
  margin-bottom: 1rem;
}

.auth-tab {
  flex: 1;
  border: none;
  background: transparent;
  padding: 0.35rem 0.5rem;
  border-radius: 999px;
  font-size: 0.85rem;
  cursor: pointer;
  color: #4b5563;
}

.auth-tab.active {
  background: #ffffff;
  box-shadow: 0 2px 8px rgba(15, 23, 42, 0.18);
  color: #1d4ed8;
  font-weight: 600;
}

/* نماذج */
.auth-form {
  display: flex;
  flex-direction: column;
  gap: 0.4rem;
  text-align: right;
}

.auth-form label {
  font-size: 0.8rem;
  margin-top: 0.2rem;
  color: #374151;
}

.auth-form input {
  width: 100%;
  padding: 0.4rem 0.6rem;
  border-radius: 10px;
  border: 1px solid #d1d5db;
  font-size: 0.85rem;
}

.auth-form input:focus {
  outline: none;
  border-color: #2563eb;
  box-shadow: 0 0 0 1px rgba(37, 99, 235, 0.3);
}

.auth-form .btn-primary {
  margin-top: 0.6rem;
  width: 100%;
}

.note {
  font-size: 0.75rem;
  color: #6b7280;
  text-align: right;
  margin: 0.4rem 0 0;
}

.auth-message {
  margin-top: 0.6rem;
  font-size: 0.8rem;
  color: #dc2626;
}

/* ================= الهيدر بعد تسجيل الدخول ================= */

.app-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: linear-gradient(135deg, #0f172a, #1e3a8a);
  padding: 0.55rem 0.9rem;
  color: #f9fafb;
  position: sticky;
  top: 0;
  z-index: 20;
  border-bottom: 3px solid #38bdf8;
}

.header-left {
  display: flex;
  align-items: center;
  gap: 0.6rem;
}

.header-logo {
  width: 42px;
  height: 42px;
  border-radius: 12px;
  object-fit: contain;
  background-color: #ffffff;
  padding: 4px;
}

.header-title {
  margin: 0;
  font-size: 1rem;
  font-weight: 700;
}

.header-subtitle {
  margin: 0.1rem 0 0;
  font-size: 0.75rem;
  color: #cbd5f5;
}

.header-right {
  display: flex;
  gap: 0.4rem;
}

/* ================= واجهة التطبيق ================= */

.app-view {
  padding: 0.75rem;
  max-width: 650px;
  margin: 0 auto;
}

/* شريط المستخدم */
.user-bar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 0.5rem;
  font-size: 0.85rem;
}

.role-badge {
  padding: 0.1rem 0.5rem;
  border-radius: 999px;
  font-size: 0.75rem;
  color: #fff;
}

/* ألوان الأدوار (سيتكرر استعمالها في الأعضاء) */
.role-dev {
  background-color: #b91c1c;  /* مطور النظام - أحمر */
}
.role-admin {
  background-color: #ea580c;  /* مدير - برتقالي */
}
.role-supervisor {
  background-color: #1e3a8a;  /* مشرف - أزرق غامق */
}
.role-member {
  background-color: #16a34a;  /* عضو - أخضر */
}

/* البحث */
.search-bar {
  margin-bottom: 0.6rem;
}

.search-bar input {
  width: 100%;
  padding: 0.45rem 0.75rem;
  border-radius: 999px;
  border: 1px solid #d1d5db;
  font-size: 0.85rem;
}

/* تبويبات رئيسية */
.main-tabs {
  display: flex;
  gap: 0.2rem;
  background-color: #e5e7eb;
  border-radius: 999px;
  padding: 0.15rem;
  margin-bottom: 0.7rem;
}

.tab-btn {
  flex: 1;
  border: none;
  background: transparent;
  padding: 0.35rem 0.4rem;
  border-radius: 999px;
  font-size: 0.8rem;
  cursor: pointer;
  color: #4b5563;
}

.tab-btn.active {
  background-color: #ffffff;
  color: #1d4ed8;
  font-weight: 600;
  box-shadow: 0 2px 8px rgba(15, 23, 42, 0.2);
}

/* محتوى التبويبات */
.tab-content {
  background-color: #ffffff;
  border-radius: 14px;
  box-shadow: 0 8px 16px rgba(15, 23, 42, 0.08);
  padding: 0.8rem;
  margin-bottom: 0.9rem;
  min-height: 100px;
}

/* شريط علوي داخل كل تبويب (العنوان + زر إضافة) */
.tab-inner-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 0.5rem;
}

.tab-inner-header h3 {
  margin: 0;
  font-size: 0.95rem;
}

/* أكورديون عام */
.accordion-list {
  display: flex;
  flex-direction: column;
  gap: 0.55rem;
}

.accordion-item {
  background-color: #f9fafb;
  border-radius: 12px;
  border: 1px solid #e5e7eb;
  overflow: hidden;
}

.accordion-header {
  padding: 0.5rem 0.65rem;
  display: flex;
  justify-content: space-between;
  align-items: center;
  cursor: pointer;
  background: linear-gradient(135deg, #eff6ff, #e0f2fe);
}

.accordion-header-main {
  display: flex;
  flex-direction: column;
  gap: 0.1rem;
}

.accordion-title {
  font-size: 0.9rem;
  font-weight: 600;
  color: #111827;
}

.accordion-subtitle {
  font-size: 0.75rem;
  color: #4b5563;
}

.accordion-meta {
  font-size: 0.7rem;
  color: #6b7280;
}

.accordion-toggle {
  font-size: 1rem;
  color: #4b5563;
}

.accordion-body {
  padding: 0.55rem 0.65rem 0.7rem;
  border-top: 1px solid #e5e7eb;
  font-size: 0.8rem;
}

.accordion-row {
  display: flex;
  justify-content: space-between;
  gap: 0.5rem;
  margin-bottom: 0.25rem;
}

.accordion-row .label {
  color: #6b7280;
}

.accordion-row .value {
  font-weight: 500;
}

.accordion-actions {
  margin-top: 0.5rem;
  display: flex;
  flex-wrap: wrap;
  gap: 0.35rem;
}

/* تمييز تعديل */
.badge-edited {
  display: inline-block;
  margin-inline-start: 0.3rem;
  padding: 0.05rem 0.4rem;
  border-radius: 999px;
  background-color: #facc15;
  color: #1f2937;
  font-size: 0.7rem;
}

.edited-original {
  margin-top: 0.35rem;
  font-size: 0.7rem;
  color: #b91c1c;
}

/* تحذير تواريخ الأسطول */
.badge-warning {
  display: inline-block;
  margin-top: 0.35rem;
  padding: 0.1rem 0.5rem;
  border-radius: 999px;
  background-color: #f97316;
  color: #fff;
  font-size: 0.75rem;
}

/* الفوتر */
.footer {
  text-align: center;
  font-size: 0.75rem;
  color: #6b7280;
  padding: 0.7rem 0.5rem 1rem;
}

/* استجابة للموبايل */
@media (max-width: 600px) {
  .app-header {
    flex-wrap: wrap;
    gap: 0.4rem;
  }

  .header-right {
    width: 100%;
    justify-content: flex-end;
  }

  .app-view {
    padding-inline: 0.6rem;
  }
}
